run_name: v2_spc_hgt_seeds
output_dir: experiments/runs
processed_dir: data/processed
models:
  - name: spc_hgt
    label: spc_hgt_v2_s43
    config: { device: cuda, epochs: 60, patience: 8, seeds: [43] }
  - name: spc_hgt
    label: spc_hgt_v2_s44
    config: { device: cuda, epochs: 60, patience: 8, seeds: [44] }
