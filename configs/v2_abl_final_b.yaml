run_name: v2_abl_final_b
output_dir: experiments/runs
processed_dir: data/processed
models:
  - name: spc_hgt
    label: final_wo_text
    config: { device: cuda, use_hard_neg: false, use_text: false, epochs: 60, patience: 8, seeds: [42] }
  - name: spc_hgt
    label: final_wo_id
    config: { device: cuda, use_hard_neg: false, use_id: false, epochs: 60, patience: 8, seeds: [42] }
