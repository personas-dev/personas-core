run_name: v2_final
output_dir: experiments/runs
processed_dir: data/processed
models:
  - name: spc_hgt
    label: spc_hgt_v2_final
    config: { device: cuda, use_hard_neg: false, epochs: 60, patience: 8, seeds: [43, 44] }
